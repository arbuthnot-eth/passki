## Abra captured

Opens the `Abra` Pokemon line tracking `.SKI`'s application to Superteam's
agentic engineering grant. Scaffold only — no runtime code touched.

## Evolution path

| Form     | Level    | Meaning                                |
|----------|----------|----------------------------------------|
| Abra     | 1-15     | Draft the application                  |
| Kadabra  | 16-35    | Submitted, evaluation in progress      |
| Alakazam | 36+      | Funded, deployed into the agent fleet  |

This PR captures Abra at **Lv4 Kinesis**. Next level (Lv3 Psybeam —
run the draft through a `solana.new` Claude sesh) is tracked in #133.

## Commits on this branch

- `Abra Lv1 Teleport` — branch scaffold + `docs/pokemon/abra.md`
- `Abra Lv2 Confusion` — gather session evidence from the Registeel wave
- `Abra Lv3 Hidden Power` — draft `application-draft.md`
- `Abra Lv4 Kinesis` — Drive upload manifest

## What the application is about

Demonstrates agentic engineering capability using **this session's** work
as evidence (parent `4ee5f75` Eevee Focus Energy, head `c788108` Registeel
Hammer Arm, 9 commits total). Headline artifacts:

1. Browser-driven IKA dWallet DKG for `ultron.sui` across ed25519 + secp256k1
   with a deterministic seed and target-scoped skip check (commits
   `2d470eb`, `2b56706`).
2. End-to-end verified sweep of the legacy `sol@ultron` address:
   $136.47 USDC + $90.75 iUSD moved to an IKA-native recipient on mainnet
   (commit `5898697`).
3. **IKA WASM inside a Durable Object** (commit `b449dd3`) — falsifies
   two joint claims from `MEMORY.md` / `feedback_ika_client.md` that
   said server-side DKG from a DO was impossible. Single most important
   artifact in the application — the "speculative plan → subagent
   spike → empirical proof → memory update" loop is exactly what the
   grant would fund.
4. Vector-principles intent authentication helper applied to
   `send-iusd-v2` (commit `e93062e`).
5. Four concurrent subagent lanes at peak, including this PR itself.

Full evidence in `docs/pokemon/abra/session-evidence.md`. Draft in
`docs/pokemon/abra/application-draft.md`. Drive manifest in
`docs/pokemon/abra/drive-upload-instructions.md`.

## Related issues

- #133 — Abra Lv3 Psybeam — run draft through solana.new Claude sesh
- #134 — Abra Lv4 Kinesis — assemble Drive folder
- #135 — Abra Lv6 Future Sight — submit Superteam form

## Review gate

Per `feedback_mainnet_review_gate.md` this is a greenfield docs-only
addition, so it should be safe to evolve once reviewed. **Do not merge
until a human approves** — leaving open per the convention.
