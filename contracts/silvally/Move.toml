[package]
name = "silvally"
edition = "2024.beta"
version = "0.0.1"

# Depend on the real IKA Move source. ika is open source; the shim
# approach broke Sui 1.69.2's upgrade-graph resolution.

[dependencies]
ika_dwallet_2pc_mpc = { git = "https://github.com/dwallet-labs/ika.git", subdir = "deployed_contracts/mainnet/ika_dwallet_2pc_mpc", rev = "mainnet-v1.1.8" }

[addresses]
ski = "0x0"

[environments]
mainnet = { chain-id = "35834a8a" }
testnet = { chain-id = "4c78adac" }
